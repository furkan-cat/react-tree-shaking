export function randomInteger1(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }

  export function randomInteger2(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }

  export function randomInteger3(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }